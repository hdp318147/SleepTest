// ICommand.aidl
package com.vivo.hdp.sleep;

// Declare any non-default types here with import statements

interface ICommand {
    /**
     * Demonstrates some basic types that you can use as parameters
     * and return values in AIDL.
     */
    String exces(String cmd);
}
